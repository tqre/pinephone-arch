# P-boot bootloader for Pinephone 1.2 and Arch Linux ARM
# Creator: Ondřej Jirman (Megous)

# Maintainer: Tuomo Kuure <tqre@far.fi>

pkgname=p-boot
pkgver=1
pkgrel=1
pkgdesc='P-boot - ultra fast bootloader for PinePhone'
arch=('aarch64')
license=('GPL')
url='https://xnux.eu/p-boot'
source=('https://megous.com/git/p-boot')

check() {
    echo "check commands"
}

package() {
    echo "package commands"
}
